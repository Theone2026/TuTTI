// src/components/Header.tsx
import React from "react";

const Header: React.FC = () => {
  return (
    <header>
      <div className="logo">Tutti Tienda</div>
    </header>
  );
};

export default Header;